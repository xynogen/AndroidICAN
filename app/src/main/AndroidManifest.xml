<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.polsri.ican">

    <uses-permission android:name="android.permission.INTERNET" />

    <uses-permission-sdk-23 android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_polsri"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_polsri"
        android:supportsRtl="true"
        android:theme="@style/Theme.Ican">
        <activity
            android:name=".menu.CameraView"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true"></activity>
        <activity android:name=".menu.kondisiAir" />
        <activity android:name=".menu.settings" />
        <activity android:name=".menu.prediction" />
        <activity android:name=".menu.beriMakan" />
        <activity android:name=".splash.splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>